cmake_minimum_required(VERSION 3.28)
project(preview-layout-tests LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
find_package(Qt6 REQUIRED COMPONENTS Core Widgets)
enable_testing()
add_executable(preview-layout-test layout-test.cpp)
target_include_directories(preview-layout-test PRIVATE ../../frontend)
target_link_libraries(preview-layout-test PRIVATE Qt6::Widgets)
add_test(NAME preview-layout COMMAND preview-layout-test)
set_tests_properties(preview-layout PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen" TIMEOUT 30)
