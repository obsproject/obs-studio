{
  "name": "aerium-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "db:migrate": "node --env-file=.env.production.local scripts/migrate.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "1.1.0",
    "oauth4webapi": "3.8.8"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8"
  }
}
