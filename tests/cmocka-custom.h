#include <stdarg.h>
#include <stddef.h>
#include <setjmp.h>

/* If we want a custom standard library here
 * for the purpose of tracing/debugging, that
 * is possible by replacing the above standard
 * headers. */

#include <cmocka.h>