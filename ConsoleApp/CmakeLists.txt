project(ConsoleApp)

include_directories(SYSTEM "${CMAKE_SOURCE_DIR}/libobs")

aux_source_directory(. SRC_LIST)
add_executable(ConsoleApp ${SRC_LIST})

target_link_libraries(${PROJECT_NAME} libobs)

install_obs_core(${PROJECT_NAME})
